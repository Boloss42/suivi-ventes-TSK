# Archive — anciennes migrations SQLite

Ce dossier contient l'historique des migrations générées lorsque le projet
utilisait SQLite en développement. Le projet est passé à PostgreSQL (dev et
prod, voir le README principal) ; ces fichiers ne sont plus utilisés par
Prisma (qui ne lit que `prisma/migrations/`) et sont conservés uniquement à
titre de référence historique.
